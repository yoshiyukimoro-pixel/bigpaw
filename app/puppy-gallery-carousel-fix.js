(()=>{
  // Intentionally disabled. BIG PAW now uses the built-in stable puppy gallery.
  // Keeping this file as a harmless no-op protects older cached pages that may
  // still request it during a deployment transition.
  window.__BIGPAW_PUPPY_GALLERY_CAROUSEL_FIX__=true;
})();
